---
- name: "install docker"
  hosts: haex.space
  become: true
  tasks:
    - name: install docker
      yum:
        name: docker-ce
        state: latest

    - name: enable service
      service:
        name: docker
        enabled: true
        state: started  

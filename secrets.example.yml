secrets:
  portainer:
    admin_password: password # must be bcrypt hashed https://docs.portainer.io/advanced/cli and every $ needs to be escaped with $
  seafile:
    admin_password: password
    db_root_password: password
  gitlab:
    root_password: password
  crowdsec_bouncer:
    password: password
